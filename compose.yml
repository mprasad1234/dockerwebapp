---
version: "3"
services:
 devopsdb:
   image: shaikmustafa/harikanew:dbimage
   ports:
     - "3306:3306"
   networks:
     - mynetwork

 application:
   image: shaikmustafa/harikanew:appimage
   ports:
     - "1111:8080"
   depends_on:
     - "devopsdb"
   networks:
     - mynetwork

networks:
  - mynetwork:
      driver: overlay
